# Open Questions

Things we're not fully sure about, with the current best answer so we don't re-decide them
differently each session. Promote to `DECISIONS.md` once settled.

| # | Question | Current best answer / status |
|---|----------|------------------------------|
| 1 | Zero-conversion revenue allocation: a campaign has revenue but Σ conversions = 0. | **IMPLEMENTED (Phase 9, the proposed default):** `allocateCampaignRevenue` (`@knn/shared/money.ts`) splits by conversions → clicks → impressions → `unallocated` (held at the campaign level, no ad gets it). Tested (`money.test.ts` + the worker `attribution.test.ts` end-to-end). Active behavior; flag if you want a different rule. |
| 2 | Which real domains? Need 3: `app.*` (dashboard+API), `articles.*` (must be AdSense-approved), separate `go.*` redirect domain (keeps the article domain clean). | Not provisioned. Using `localhost` ports locally. Needed before staging. |
| 3 | Facebook App + business verification (can take days). Perms: `ads_management, ads_read, pages_show_list, pages_read_engagement, business_management`; App Review for prod. | **Phase 2 code is complete + tested (mocked Graph).** Remaining is the external app: create the FB Business app + verification, then set `FB_APP_ID/SECRET/OAUTH_REDIRECT_URI` to enable live connect. `auth-url` returns 503 until configured. Staging uses FB **test** ad accounts (D18). _Aman to own app creation._ |
| 4 | AdSense AFS / Custom Search Ads is invite/approval-gated by Google; Management API access must be granted. | **Phase 9 code is complete + tested (injected data); the live pull is DORMANT.** `@knn/adsense` (`fetchChannelReport`/`parseChannelReport`) reads per-channel daily earnings + AFS clicks from the Management API v2; attribution consumes it via an injected `fetchAdsense` dep (undefined by default → cleanly skips). To go live needs: (a) AFS reporting access + the AdSense account id, (b) a **Google OAuth token store** (see #13), and (c) confirming the live `CUSTOM_CHANNEL_ID` ↔ our `Channel.channelId` (`ch`) mapping + any AFS product filter. Until then `campaign_revenue_daily` stays empty in prod (FB cost stats still populate). **Getting real channel ids (to replace the seed placeholders) — researched 2026-05-28:** create them in the AdSense UI (Reports → Settings → Manage custom channels → Add channel → product **Search**; cap **500 channels/product/account**), then auto-seed our pool via Management API v2 `accounts.adclients.customchannels.list` (works with the `adsense.readonly` scope). The API `customchannels.create` exists but is **restricted to AdSense-for-Platforms (AFP) partner projects** — so for normal accounts, creation is manual UI. **Scale flag:** the 500/product cap vs the 1500–2000-campaign target means the channel **pool/reuse** model (D11, concurrent ≤500) is load-bearing; at real scale the path is multiple/per-tenant AdSense accounts or becoming an **AFP partner** (which also unlocks programmatic channel creation). AFS click-derived metrics <10/day report as 0 — matches our existing `<10` suppression (Phase 9). |
| 5 | FB token model: also offer the Business-Manager partner / system-user path (non-expiring) vs per-user OAuth (~60d churn)? | Default v1 = per-user OAuth long-lived tokens with `CONNECTION_BROKEN` handling (D13). Partner/system-user is the durable upgrade for scale. |
| 6 | Revenue-cut authority: does a Company-Admin take a further cut from their buyers (2-level margin)? | Default v1 = single-level (platform/Super-Admin sets the cut per company/buyer). |
| 7 | Final Facebook Graph API version to pin. | Pinned `FB_API_VERSION=v21.0` for Phase 2 (configurable via env). Re-confirm against the then-current stable before prod App Review. |
| 8 | Playwright E2E — the Phase 3 gate names a browser E2E ("build a 2-adset/4-ad campaign → draft → submit"); none exists yet. | **Substituted for now** by API integration tests (`campaigns.test.ts`/`approval.test.ts`, real PG) + manual in-browser verification (Preview MCP). A real Playwright harness (config + browser deps + seeded authed buyer w/ mocked FB assets, wired into CI) is deferred — _decide with Aman: build it now, or formally accept the substitution and move the E2E gate to a later hardening pass._ |
| 9 | Traffic split (`campaign_traffic_splits`) — listed under the Phase 3/7 data-model. | **Half-built:** the weighted-split LOGIC is implemented + tested (`apps/redirect/src/resolve.ts#pickSplit` + `splits` in `RedirectConfig`), but **no data is wired** — `launchCampaign` never writes `splits` to KV and the `campaign_traffic_splits` table was never created. So it's unreachable end-to-end. Finish = a campaign split data model + UI + have launch write `splits`. Deferred (not a regression). |
| 10 | **Auto-launch chain (Phase 8).** approve → `enqueueChannelAssign` → worker assigns channel → PROCESSING. Should it then auto-create on FB, or wait for a manual launch? | **RESOLVED (D19): per-company `Organization.autoLaunch` toggle**, default OFF (manual gate). When ON, the worker fires `triggerAutoLaunch` the moment a campaign acquires a channel (direct assign + queue-drain + rollover paths) → enqueues `FB_LAUNCH` → the worker POSTs the token-guarded `POST /api/internal/launch/:id` (launch must run on the API — it owns the FB client + on-disk creatives). Idempotent via `fbCampaignId`; `FB_LAUNCH` is `attempts:1` so a partial FB failure can't double-create. Toggle UI sits next to auto-approve on the Approvals page; notify wording is now launch-mode-aware. **Follow-up (Phase 11 hardening):** make `launchCampaign` resumable on *partial* FB creation (track created campaign/adset/ad ids) so a failed launch can be safely retried instead of needing a manual re-launch. Requires `INTERNAL_API_TOKEN` + `INTERNAL_API_URL` set in deployed envs (see `.env.example`). |
| 11 | **Domain-env alignment for the live funnel (Phase 8, MEDIUM).** `launchCampaign` builds the FB creative link as `${REDIRECT_DOMAIN}/go/{id}` and article URL as `${ARTICLE_DOMAIN}/a/{slug}`. The live funnel + Worker + AFS are on **`go.10linesabout.com` / `articles.10linesabout.com`**, but committed envs are placeholders/`rsoc.app`. | Before launching a real campaign on the 10linesabout funnel, the deployed `.env` must set `REDIRECT_DOMAIN=https://go.10linesabout.com` + `ARTICLE_DOMAIN=https://articles.10linesabout.com`. Long-term this is **per-org/per-domain** (different clients → different approved domains) — a multi-domain model, not a single env. |
| 12 | **Legacy Node redirect still built + in the staging compose (Phase 7).** `apps/redirect/src/{app,index}.ts` did a param-less 302 with no AFS params/split; the real redirect is the Cloudflare Worker on `go.*`. | **RESOLVED (task #18):** retired entirely — removed the `redirect` service + caddy `depends_on` from `deploy/docker-compose.staging.yml`, the `{$REDIRECT_HOST}` Caddy block, and `REDIRECT_HOST`/`REDIRECT_PORT` from the staging env example; deleted `app.ts`/`index.ts`/`app.test.ts` + `tsup.config.ts`; the `@knn/redirect` package is now Worker-only (no `build`/`start`; deploys via `wrangler`). A stray route flip to the box now fails loud (no cert/host) instead of serving unmonetized 302s. `REDIRECT_DOMAIN` must point at the Worker host (`go.*`). |
| 13 | **Google OAuth connection + AdSense token store (Phase 9 follow-up).** Attribution's AdSense pull needs a Google OAuth access token (Management scope), but there's no Google-connect flow / token model yet (only `GOOGLE_CLIENT_ID/SECRET` env exist). | **BUILT (D22, 2026-05-28):** `@knn/adsense` Google OAuth (offline refresh token, read-only) + `GoogleConnection` global singleton (encrypted token) + super-admin connect flow (`/api/adsense/*`, Connect-AdSense card) + `syncChannels` (real AFS channel ids → pool). The worker's `fetchAdsense` is now `liveAdsenseFetch` (self-dormant; auto-activates on connect). **Remaining = external only:** AFS Management API access (#4) + setting `GOOGLE_CLIENT_ID/SECRET` + the `…/api/adsense/callback` redirect URI. Until those land, the live pull no-ops and revenue stays unallocated in prod. |
| 14 | **FB insight day buckets are the ad-account timezone, not strictly IST (Phase 9, LOW).** `ad_stats_daily.day` uses FB's `date_start` (the account's reporting tz); AdSense/revenue days are IST (D4). For an IST ad account (e.g. the staging test account) they coincide; for other-tz accounts the day can shift by the offset, slightly skewing the join. | Acceptable approximation for v1. Exact alignment needs FB hourly breakdown re-bucketed into IST days — a refinement if non-IST accounts become common. |
